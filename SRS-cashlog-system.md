# เอกสารข้อกำหนดและสเปกระบบ (SRS)
## CashLog — ระบบบันทึกรายรับ-รายจ่ายส่วนตัว (ภาพรวมทั้งระบบ)

**เวอร์ชัน:** 2.0
**ผู้เขียน:** Jaruvat (Jar)
**วันที่:** 4 กันยายน 2569 (September 2026) — ปรับปรุงจากฉบับ 26 สิงหาคม 2569

เอกสารฉบับนี้ปรับปรุงจาก SRS v1 (26 สิงหาคม 2569) โดยรวมผลจากกระบวนการ grill-me กับ Claude ระหว่างออกแบบสถาปัตยกรรม Flutter frontend และการตรวจสอบเทียบกับโค้ด backend จริง (branch `main`) ที่อาจพัฒนาไปมากกว่าฉบับเดิม การเปลี่ยนแปลงทั้งหมดถูกทำเครื่องหมายไว้ในเนื้อหาด้วย:
- 🆕 **v2:** ของใหม่ที่ไม่มีใน v1
- ✏️ **แก้ไขจาก v1:** ของเดิมที่ถูกปรับให้ตรงกับพฤติกรรมจริง

---

## ประวัติการแก้ไข (Revision History)

| เวอร์ชัน | วันที่ | สรุปการเปลี่ยนแปลง |
|---|---|---|
| v1 | 26 ส.ค. 2569 | ฉบับแรก — ครอบคลุม backend baseline และแผน Account/Transfer/Manual Entry/Auth/Frontend |
| v2 | 4 ก.ย. 2569 | (1) แก้ BR-8/FR-4.8 ให้ตรงกับพฤติกรรมจริง — backend ไม่ skip เงียบๆ, frontend เป็นผู้กรอง/แจ้งเตือนแทน (2) แก้ Account schema จาก `icon_key`+`color_hex` เป็น `bank_icon` เดียว (3) เพิ่ม BR-10 (account cache sync ต้อง upsert-only) (4) เพิ่ม BR-11 (quick category assignment ผ่าน chip บนหน้า feed) (5) ยืนยัน rate limit ตัวเลขจริงจากโค้ดและเพิ่มข้อกำหนดการ throttle ฝั่ง client (6) เพิ่มรายละเอียด local persistence (drift) และ error-handling pattern (dartz `Either`) ของ frontend |

---

## สารบัญ

1. [บทนำ](#1-บทนำ)
2. [เป้าหมายของระบบ](#2-เป้าหมายของระบบ-objectives)
3. [ขอบเขตของระบบ](#3-ขอบเขตของระบบ-scope)
4. [ผู้ใช้งานและบทบาท](#4-ผู้ใช้งานและบทบาท-actors)
5. [สถาปัตยกรรมระบบ](#5-สถาปัตยกรรมระบบ-system-architecture)
6. [แบบจำลองข้อมูล](#6-แบบจำลองข้อมูล-data-model)
7. [กฎทางธุรกิจ](#7-กฎทางธุรกิจ-business-rules)
8. [ความต้องการเชิงฟังก์ชัน](#8-ความต้องการเชิงฟังก์ชัน-functional-requirements)
9. [รายการ API Endpoint](#9-รายการ-api-endpoint)
10. [ความต้องการที่ไม่ใช่เชิงฟังก์ชัน](#10-ความต้องการที่ไม่ใช่เชิงฟังก์ชัน-non-functional-requirements)
11. [แผนสำหรับเฟสถัดไป](#11-แผนสำหรับเฟสถัดไป-future-scope)
12. [ภาคผนวก: Design Decision Log](#12-ภาคผนวก-บันทึกการตัดสินใจออกแบบ-design-decision-log)

---

## 1. บทนำ

### 1.1 วัตถุประสงค์ของเอกสาร
เอกสารนี้เป็น Software Requirement & Specification (SRS) ฉบับรวมทั้งระบบของ CashLog ครอบคลุมทั้งส่วนที่พัฒนาไว้แล้ว (backend `cashlog-api`) และส่วนที่พัฒนาเพิ่ม (Account/Transfer, Manual Entry, Auth, Flutter frontend) เพื่อให้เป็นเอกสารอ้างอิงเดียวที่สมบูรณ์สำหรับทีมพัฒนา (ในที่นี้คือผู้พัฒนาคนเดียว) และเครื่องมือ AI ที่ใช้ช่วยเขียนโค้ด (Claude Code, GitHub Copilot)

### 1.2 ภาพรวมระบบ
CashLog เป็นแอปพลิเคชันบันทึกรายรับ-รายจ่ายส่วนบุคคล จุดเด่นคือการอ่านข้อมูลจากภาพสลิปโอนเงิน/ใบเสร็จด้วย AI (Gemini) เพื่อบันทึกธุรกรรมโดยอัตโนมัติ ลดภาระการพิมพ์ข้อมูลเอง ระบบยังรองรับการติดตามยอดเงินแยกตามบัญชี (เช่น บัญชีใช้จ่าย, บัญชีออม/ลงทุน) และสรุปผลรายเดือน/รายปี

### 1.3 ผู้ใช้งานระบบ
ระบบออกแบบสำหรับผู้ใช้คนเดียว (single-user) คือเจ้าของระบบเอง ไม่มีแนวคิดเรื่องบัญชีผู้ใช้หลายคนหรือการแชร์ข้อมูลกับผู้อื่นในเวอร์ชันนี้

### 1.4 ขอบเขตของเอกสาร
เอกสารนี้ครอบคลุมทั้งสิ่งที่มีอยู่แล้วในโค้ด `cashlog-api` (อธิบายในฐานะ baseline ของระบบ) และสิ่งที่จะพัฒนาเพิ่มเติม เพื่อให้ผู้อ่านเห็นภาพระบบที่สมบูรณ์ในเวอร์ชันเป้าหมาย (CashLog v2) โดยไม่ต้องอ้างอิงเอกสารอื่นประกอบ

> เอกสารฉบับนี้อ้างอิงร่วมกับเอกสารสถาปัตยกรรม frontend แยกต่างหาก (`cashlog-frontend-spec.md` และ `cashlog-frontend-tickets.md`) ซึ่งลงรายละเอียดระดับ implementation ที่ SRS ฉบับนี้ไม่ลงลึกถึง

---

## 2. เป้าหมายของระบบ (Objectives)

1. บันทึกรายการรายรับ รายจ่าย และการย้ายเงินระหว่างบัญชีได้อย่างรวดเร็วและครบถ้วน
2. ลดภาระการพิมพ์ข้อมูลด้วยการอ่านสลิปอัตโนมัติผ่าน AI
3. ติดตามยอดเงินคงเหลือแยกตามบัญชีที่ใช้งานจริง
4. สรุปภาพรวมการเงินรายเดือน/รายปีแยกตามหมวดหมู่ ให้เห็นพฤติกรรมการใช้จ่าย
5. เน้นความเร็วและความครบถ้วนของการบันทึกข้อมูลมากกว่าความแม่นยำสมบูรณ์แบบ 100% ในทุกรายการ (ยอมรับการแก้ไขย้อนหลังในส่วนน้อยที่ผิดพลาด)

---

## 3. ขอบเขตของระบบ (Scope)

### 3.1 สิ่งที่อยู่ในขอบเขต (In Scope)
- การจัดการบัญชีเงิน (Account): สร้าง แก้ไข ปิดการใช้งาน และดูยอดคงเหลือ
- การจัดการหมวดหมู่ (Category): สร้าง แก้ไข ลบ
- การบันทึกธุรกรรม 3 ประเภท: รายรับ (income), รายจ่าย (expense), ย้ายเงิน (transfer) — ผ่านการสแกนสลิปอัตโนมัติ (AI OCR) หรือผ่านการกรอกข้อมูลเอง (manual entry)
- การแก้ไขและลบธุรกรรมย้อนหลัง
- แดชบอร์ดสรุปผลรายเดือนและรายปี
- ระบบยืนยันตัวตนอย่างง่ายด้วย API Key
- แอปพลิเคชัน Flutter สำหรับใช้งานบนมือถือ (ขอบเขต v2: **Android เท่านั้น** — ดู 3.2)

### 3.2 สิ่งที่อยู่นอกขอบเขต (Out of Scope)
- ระบบสมาชิกหลายคน (multi-user), การล็อกอินแบบเต็มรูปแบบ (username/password, OAuth)
- การวิเคราะห์ผลตอบแทนการลงทุน (กำไร/ขาดทุน, portfolio performance) — เตรียมโครงสร้างข้อมูลรองรับไว้เท่านั้น
- การประมวลผลสลิปแบบ asynchronous ผ่าน job queue
- หน้าจอ "รายการรอตรวจสอบ" หรือสถานะ draft/pending ของธุรกรรม
- การรองรับหลายสกุลเงิน (multi-currency)
- เว็บแอปพลิเคชันหรือเดสก์ท็อปแอป

> ✏️ **แก้ไขจาก v1:** v1 เขียนว่า "รองรับเฉพาะมือถือ (Android/iOS)" — v2 จำกัดเหลือ **Android เท่านั้น** เนื่องจากผู้ใช้จริงมีเพียงเครื่อง Android และการสแกน gallery แบบ auto-scan (BR-8) ต้องพึ่งพา MediaStore/album ที่เป็นกลไกเฉพาะของ Android ทำให้ยังไม่คุ้มที่จะออกแบบรองรับ iOS ในเวอร์ชันนี้ (iOS มี Limited Photo Library ที่ขัดกับ auto-scan โดยตรง)

---

## 4. ผู้ใช้งานและบทบาท (Actors)

| Actor | คำอธิบาย |
|---|---|
| Owner (เจ้าของระบบ) | ผู้ใช้เพียงคนเดียวของระบบ เข้าถึงทุกฟังก์ชันผ่านแอป Flutter ที่ผูก API Key ไว้ล่วงหน้า |

---

## 5. สถาปัตยกรรมระบบ (System Architecture)

### 5.1 ภาพรวมสถาปัตยกรรม

```
[ Flutter App ] --HTTPS/REST--> [ Go/Fiber API ] --> [ PostgreSQL ]
                                        |
                                        +--> [ Redis (cache) ]
                                        |
                                        +--> [ Gemini AI (slip OCR) ]
```

- **Frontend:** Flutter (Android), Clean Architecture แบบ feature-first, Riverpod (+ `riverpod_generator`) สำหรับ state management, dartz (`Either<Failure, T>`) สำหรับจัดการ error, `go_router` สำหรับ routing, `drift` สำหรับ local persistence, ไอคอนจาก Remix Icon
- **Backend:** Go + Fiber framework, Clean Architecture (domain -> usecase -> repository -> delivery/http)
- **Database:** PostgreSQL ผ่าน GORM (ไม่ใช้ Supabase PostgREST เป็น API layer — GORM คุย Postgres โดยตรง)
- **Cache:** Redis สำหรับ cache ผลสรุปแดชบอร์ดและป้องกันการสแกนสลิปซ้ำ (เชื่อมต่อผ่าน `REDIS_URL` เดียว)
- **AI:** Gemini AI (`gemini-2.5-flash`) สำหรับอ่านข้อมูลจากภาพสลิป

> ✏️ **แก้ไขจาก v1:** v1 ระบุ config ตัวแปร Redis แยกเป็น `REDIS_HOST`/`REDIS_USERNAME`/`REDIS_PASSWORD`/`REDIS_DB` — โค้ดจริงรวมเป็นตัวแปรเดียว `REDIS_URL` แล้ว

### 5.2 โครงสร้างโค้ด Backend (Clean Architecture)

```
cmd/api/                        - entry point และ dependency injection
cmd/config/                     - โหลดค่า configuration จาก .env
internal/domain/                - entity และ interface
internal/usecase/                - business logic
internal/repository/postgres/    - เข้าถึงข้อมูลจริงผ่าน GORM
internal/repository/redis/       - เข้าถึง Redis cache
internal/infrastructure/gemini/  - client เรียก Gemini AI
internal/delivery/http/          - HTTP layer (Fiber): router, middleware, handler, DTO
pkg/                             - shared utilities (logger, timeutil, validate, response)
```

### 5.3 โครงสร้างโค้ด Frontend (Flutter, Clean Architecture feature-first)

```
lib/
  core/       - network client (dio), error handling (Either/Failure),
              local db (drift), env config, router (go_router)
  features/
    account/     - จัดการบัญชี
    category/    - จัดการหมวดหมู่
    transaction/ - รายการธุรกรรม (list, create, edit, quick-category)
    slip_scan/   - สแกนสลิปอัตโนมัติ + manual upload
    dashboard/   - แดชบอร์ดสรุปผล
  shared/     - reusable widgets
```

แต่ละ feature แบ่งเป็น 3 layer: presentation (UI + Riverpod providers), domain (entity + usecase + repository interface), data (repository implementation + data source เรียก API)

> 🆕 **v2:** รายละเอียดระดับ implementation ของ frontend (drift schema เต็ม, sequence diagram ของ auto-scan, dependency ระหว่าง ticket) อยู่ในเอกสารแยก `cashlog-frontend-spec.md` และ `cashlog-frontend-tickets.md`

### 5.4 Local Persistence (Frontend)

Frontend ใช้ `drift` (typed SQLite) เก็บข้อมูล 3 กลุ่ม:
1. read-through cache ของ transactions/accounts/categories จาก API เพื่อรองรับการอ่านแบบออฟไลน์
2. ตาราง `scanned_slips` สำหรับ diff ไฟล์ที่เคยสแกนแล้วและ retry queue ของสลิปที่ล้มเหลว
3. ตาราง `pending_manual_actions` สำหรับ retry ของ manual create/update/delete ที่ล้มเหลวจาก network

**กลยุทธ์ offline:** online-only + read cache — การสร้าง/แก้ไขข้อมูลต้องมีการเชื่อมต่ออินเทอร์เน็ต (เนื่องจาก core flow ต้องพึ่งพา Gemini ผ่าน backend อยู่แล้ว) ส่วนการอ่านข้อมูลรองรับออฟไลน์ผ่าน cache

---

## 6. แบบจำลองข้อมูล (Data Model)

### 6.1 Account (บัญชีเงิน)

> ✏️ **แก้ไขจาก v1:** v1 มี `icon_key` (string) + `color_hex` (string) แยกกัน 2 ฟิลด์ — v2 รวมเป็น `bank_icon` ฟิลด์เดียว (string, จำเป็น, ไม่เกิน 100 ตัวอักษร, ไม่มี enum บังคับฝั่ง backend) เก็บเป็น code เช่น `"scb"`, `"dime"` เพื่อระบุธนาคาร ส่วนชื่อธนาคารแบบเต็ม โลโก้ และสีประจำธนาคาร ให้ Frontend เป็นเจ้าของข้อมูลทั้งหมดผ่าน local static map ในแอป (ไม่ persist สีลง backend อีกต่อไป) เหตุผล: ลดความซับซ้อนของ backend, ให้ Frontend ควบคุม UI/branding ได้อิสระโดยไม่ต้อง migrate ข้อมูลเก่าเมื่อปรับดีไซน์

| Field | Type | คำอธิบาย |
|---|---|---|
| id | int64 (PK) | รหัสบัญชี |
| name | string | ชื่อบัญชี เช่น "SCB", "Dime", "Dime-FCD" |
| account_type | enum: cash, bank, investment, ewallet | ประเภทบัญชี (เตรียมค่า investment รองรับอนาคต) |
| opening_balance | numeric(12,2) | ยอดเงินตั้งต้นตอนเริ่มบันทึกในระบบ |
| matching_keywords | array of string | คำ/ชื่อธนาคารที่ใช้จับคู่อัตโนมัติจากข้อความบนสลิป |
| **bank_icon** | string | รหัสธนาคาร (เช่น `"scb"`, `"dime"`) — Frontend map เป็นชื่อ/โลโก้/สีเอง ไม่มี enum ฝั่ง backend |
| is_active | boolean | true = ใช้งานอยู่, false = ปิดใช้งานแล้ว (soft delete) |
| created_at, updated_at | timestamp | เวลาสร้าง/แก้ไขล่าสุด |

ยอดคงเหลือ (`current_balance`) เป็นค่าที่คำนวณสด (derived) ไม่เก็บเป็น column ตรงๆ ในฐานข้อมูล:

```
current_balance(account) =
  opening_balance
  + SUM(amount ของ income ที่ account_id = account)
  - SUM(amount ของ expense ที่ account_id = account)
  - SUM(amount ของ transfer ที่ from_account_id = account)
  + SUM(amount ของ transfer ที่ to_account_id = account)
```

### 6.2 Category (หมวดหมู่)

| Field | Type | คำอธิบาย |
|---|---|---|
| id | int64 (PK) | รหัสหมวดหมู่ |
| name | string | ชื่อหมวดหมู่ (unique) |
| type | enum: income, expense | ประเภทหมวดหมู่ (ธุรกรรมประเภท transfer ไม่ผูกกับ category ก็ได้) |
| icon_key | string | รหัสไอคอน |
| color_hex | string | สีประจำหมวดหมู่ |
| created_at, updated_at | timestamp | |

### 6.3 Transaction (ธุรกรรม)

| Field | Type | คำอธิบาย |
|---|---|---|
| id | uint (PK) | รหัสธุรกรรม |
| amount | numeric(12,2) | จำนวนเงิน |
| transaction_type | enum: income, expense, transfer | ประเภทธุรกรรม (แก้ไขไม่ได้หลังสร้างแล้ว — ดู Design Decision Log) |
| account_id | int64 (FK Account, nullable) | บัญชีที่เกี่ยวข้อง — ใช้กับ income/expense |
| from_account_id | int64 (FK Account, nullable) | บัญชีต้นทาง — ใช้กับ transfer เท่านั้น |
| to_account_id | int64 (FK Account, nullable) | บัญชีปลายทาง — ใช้กับ transfer เท่านั้น |
| category_id | int64 (FK Category, nullable) | หมวดหมู่ (ไม่บังคับ โดยเฉพาะ transfer หรือกรณี AI เดาประเภทไม่ได้) |
| sender_name | string | ชื่อผู้โอน (จากสลิป ถ้ามี) |
| receiver_name | string | ชื่อผู้รับ (จากสลิป ถ้ามี) |
| note | string | บันทึกเพิ่มเติม |
| local_image_name | string | ชื่อไฟล์ภาพสลิป (ใช้ป้องกันการสแกนซ้ำ ไม่ได้เก็บภาพจริง) |
| transaction_date | timestamp | วันเวลาของธุรกรรม |
| source | enum: slip, manual | ที่มาของรายการ (สแกนสลิป หรือ กรอกเอง) |
| created_at, updated_at | timestamp | |

> 🆕 **v2:** `is_junk` **ไม่ใช่** column ในฐานข้อมูล — เป็นค่าที่ Frontend คำนวณสดตอนแสดงผล (`amount == 0 && sender_name` ว่าง `&& receiver_name` ว่าง) เพื่อโชว์ badge เตือนในหน้า feed ดู BR-8 (revised) หัวข้อ 7

### 6.4 Configuration (ไม่ใช่ตารางฐานข้อมูล)

| ชื่อ | ประเภท | คำอธิบาย |
|---|---|---|
| OWNER_NAME_ALIASES | env var (array) | รายชื่อของเจ้าของระบบที่อาจปรากฏบนสลิปในรูปแบบต่างๆ ใช้จำแนกประเภทธุรกรรมอัตโนมัติ |
| API_KEY | env var | กุญแจสำหรับยืนยันตัวตนของแอป |
| GEMINI_API_KEY, MODEL_NAME | env var | การตั้งค่าเชื่อมต่อ Gemini AI (ค่า default `MODEL_NAME = gemini-2.5-flash`) |
| DB_URL, REDIS_URL | env var | การเชื่อมต่อฐานข้อมูลและ Redis |

### 6.5 แผนภาพความสัมพันธ์ข้อมูล (Entity Relationship)

- Account (1) --< (0..n) Transaction [account_id] (income / expense)
- Account (1) --< (0..n) Transaction [from_account_id] (transfer ต้นทาง)
- Account (1) --< (0..n) Transaction [to_account_id] (transfer ปลายทาง)
- Category (1) --< (0..n) Transaction [category_id] (ไม่บังคับ)

---

## 7. กฎทางธุรกิจ (Business Rules)

### BR-1: การจำแนกประเภทธุรกรรมอัตโนมัติจากสลิป
เมื่อสแกนสลิป ระบบเปรียบเทียบชื่อผู้โอน (sender_name) และผู้รับ (receiver_name) ที่ Gemini อ่านได้กับ `OWNER_NAME_ALIASES` (เทียบแบบไม่สนตัวพิมพ์เล็ก-ใหญ่และยอมรับการจับคู่บางส่วน):

| เงื่อนไข | ผลลัพธ์ |
|---|---|
| sender ตรงกับเจ้าของ, receiver ไม่ตรง | `transaction_type = expense` |
| receiver ตรงกับเจ้าของ, sender ไม่ตรง | `transaction_type = income` |
| ทั้ง sender และ receiver ตรงกับเจ้าของ | `transaction_type = transfer` |
| จับคู่ไม่ได้ทั้งสองฝั่ง | `transaction_type = expense` และ `category_id = null` |

### BR-2: การจับคู่บัญชีอัตโนมัติจากข้อความธนาคารบนสลิป
- ระบบเทียบข้อความชื่อธนาคาร/ข้อมูลที่ Gemini อ่านได้กับ `matching_keywords` ของแต่ละ Account เพื่อเติมค่า `account_id` / `from_account_id` / `to_account_id` ให้อัตโนมัติ
- การจับคู่นี้เป็นค่าที่เดาไว้ล่วงหน้าเท่านั้น ไม่รับประกันความถูกต้อง 100% เนื่องจากข้อจำกัดของข้อมูลบนสลิปจริง เช่น การโอนผ่านพร้อมเพย์ไม่แสดงชื่อธนาคารของผู้รับเป็นข้อความ (มีเพียงไอคอน), การโอนเงินระหว่างบัญชีในเครือเดียวกัน (เช่น ไปยังบัญชี FCD) อาจไม่มีข้อมูลธนาคารปรากฏเลย
- หากจับคู่ไม่ได้ให้ปล่อยค่า field บัญชีที่เกี่ยวข้องเป็น null ไว้ก่อน ผู้ใช้สามารถแก้ไขภายหลังผ่านฟังก์ชันแก้ไขธุรกรรม

### BR-3: บันทึกอัตโนมัติทันทีเสมอ (ไม่มีสถานะรอตรวจสอบ)
- ทุกรายการที่ได้จากการสแกนสลิปจะถูกบันทึกลงฐานข้อมูลทันที ไม่มีขั้นตอนยืนยันก่อนบันทึกและไม่มีสถานะ draft/pending
- หลักการออกแบบ: ให้ความสำคัญกับความเร็วและความครบถ้วนของการบันทึกข้อมูลเป็นอันดับแรก ยอมรับว่าส่วนน้อยอาจต้องแก้ไขย้อนหลัง

### BR-4: การย้ายเงิน (Transfer) ไม่นับรวมในยอดสรุปรายรับ-รายจ่าย
- ธุรกรรมประเภท transfer จะไม่ถูกรวมเข้ากับ `total_income` หรือ `total_expense` ในแดชบอร์ดสรุปผล เนื่องจากไม่ใช่รายได้หรือค่าใช้จ่ายที่แท้จริง เป็นเพียงการย้ายเงินภายในกระเป๋าของเจ้าของเอง
- แดชบอร์ดแสดงยอด `total_transfer` แยกเป็นส่วนต่างหาก

### BR-5: หมวดหมู่ไม่บังคับสำหรับการย้ายเงิน
- ธุรกรรมประเภท transfer ไม่จำเป็นต้องระบุ `category_id` เนื่องจากความหมายของรายการสื่อสารผ่านบัญชีต้นทาง-ปลายทางอยู่แล้ว

### BR-6: การป้องกันการสแกนสลิปซ้ำ
- ระบบตรวจสอบชื่อไฟล์ภาพ (`local_image_name`) ผ่าน Redis cache ก่อนประมวลผลทุกครั้ง (TTL 30 วัน) หากพบว่าเคยประมวลผลไฟล์นี้แล้วจะข้ามการสร้างธุรกรรมซ้ำ

### BR-7: การคำนวณยอดคงเหลือ
- ยอดคงเหลือของแต่ละบัญชีคำนวณแบบ real-time จากสูตรในหัวข้อ 6.1 ไม่เก็บเป็นค่าคงที่ในฐานข้อมูลเพื่อป้องกันข้อมูลไม่ตรงกัน (data inconsistency)
- ระบบต้องแสดงข้อความกำกับว่ายอดคงเหลือเป็น "ค่าประมาณจากข้อมูลที่บันทึกในระบบ" เนื่องจากอาจมีรายการที่ไม่ได้บันทึกไว้

### BR-8: การรับสลิปเข้าระบบ 2 ช่องทาง (MeowJot-style auto-scan)
- ช่องทางอัตโนมัติ: ตอนเปิดแอป (หรือกลับมาจาก background) แอปสแกนคลังภาพในเครื่องหาไฟล์ภาพใหม่ที่ยังไม่เคยประมวลผล เฉพาะใน album ของแอปธนาคารที่กำหนด (เช่น "SCB EASY", "Dime!") แล้วอัปโหลดเข้าสู่ pipeline เดียวกันโดยอัตโนมัติ ไม่ต้องกดปุ่มใดๆ (ต้องขอสิทธิ์ Photo Library)
- ช่องทางกดเอง: ปุ่มกล้องในแอปยังคงใช้งานได้ตลอดเวลาเพื่อถ่ายหรือเลือกภาพสลิปด้วยตนเอง ใช้แก้ปัญหาเฉพาะกรณีที่ภาพสลิปไม่ได้อยู่ในตำแหน่งที่ระบบสแกนอัตโนมัติถึง (พบบ่อยกับรายรับ)
- ทั้งสองช่องทางเรียกใช้ pipeline ประมวลผลเดียวกันทั้งหมด (BR-1, BR-2, BR-3, BR-6) ไม่มีความแตกต่างเชิง business logic ฝั่ง backend

> ✏️ **แก้ไขจาก v1:** v1 ระบุว่า "หากภาพที่สแกนพบไม่ใช่สลิปที่มีข้อมูลอ่านได้ ระบบต้องข้ามภาพนั้นไปโดยไม่สร้างธุรกรรมและไม่แจ้งเตือนผู้ใช้" — ข้อนี้ไม่ตรงกับพฤติกรรมจริงของ backend (Gemini ไม่ reject ภาพที่ไม่ใช่สลิป แต่คืนค่าฟิลด์ว่าง/0 แทน และ usecase ก็ยังสร้าง transaction ต่อ) และหลังพิจารณาแล้วตัดสินใจ**ไม่แก้ backend ให้ skip** เพราะเสี่ยงข้อมูลสูญหายแบบไม่รู้ตัวหาก Gemini ตีความสลิปจริงผิดพลาด กฎข้อนี้จึงถูกแก้เป็นดังนี้:
>
> - Backend สร้างธุรกรรมตามปกติสำหรับทุกภาพที่ผ่าน pipeline แม้ Gemini จะอ่านข้อมูลไม่ได้เลย (amount = 0, sender_name/receiver_name ว่างทั้งคู่)
> - Frontend เป็นผู้ตรวจจับเคสนี้ (amount == 0 && sender_name ว่าง && receiver_name ว่าง) แล้วแสดงเป็นรายการ "junk" พร้อม badge เตือนชัดเจนในหน้า feed หลัก (ไม่ซ่อน/ไม่ข้าม) พร้อมปุ่มลบหรือแก้ไขข้อมูลเองได้ทันที

### BR-9: การกำหนดหมวดหมู่แบบเร็ว (Quick Category Assignment)
- เนื่องจากการจำแนกประเภทอัตโนมัติ (BR-1) ไม่ได้ระบุหมวดหมู่ให้เลย ธุรกรรมที่มาจากสลิปเกือบทั้งหมดจะไม่มีหมวดหมู่ตั้งแต่แรก ผู้ใช้ต้องเข้ามากำหนดหมวดหมู่เองในภายหลัง
- ระบบต้องรองรับการกำหนดหมวดหมู่แบบรวดเร็วที่สุดเท่าที่เป็นไปได้ โดยไม่ต้องผ่านหน้าจอแก้ไขแบบเต็มรูปแบบ และไม่ต้องเพิ่ม endpoint ใหม่ (ใช้กลไกแก้ไขบางส่วนของ endpoint แก้ไขธุรกรรมที่มีอยู่แล้ว)

### BR-10: การซิงค์ cache บัญชีฝั่ง Frontend ต้องเป็น upsert-only 🆕
- Endpoint `GET /accounts` คืนเฉพาะบัญชีที่ `is_active = true` เท่านั้น บัญชีที่ถูกปิดใช้งานแล้ว (soft delete) จะไม่ปรากฏใน response อีกต่อไป
- การซิงค์ local cache ของ Frontend (drift) จาก response นี้ต้องเป็น **upsert เท่านั้น** (เพิ่ม/อัปเดตตามที่ได้รับ แต่**ไม่ลบ** record ที่หายไปจาก response) เพื่อให้ยังสามารถแสดงชื่อและโลโก้ของบัญชีที่ปิดไปแล้วได้ถูกต้อง เมื่อบัญชีนั้นยังถูกอ้างอิงอยู่ในธุรกรรมเก่า
- หน้าจอที่ต้องแสดงเฉพาะบัญชีที่ active (เช่น หน้าเลือกบัญชีตอนสร้างธุรกรรมใหม่) ให้ filter ตอน**query**จาก local cache แทนที่จะ filter ตอน sync

### BR-11: การกำหนดหมวดหมู่ผ่าน chip บนหน้า feed หลัก 🆕
- พฤติกรรมหลักของ BR-9 คือการ tap ปุ่ม/chip แสดงหมวดหมู่ที่อยู่ในแถวของธุรกรรมบนหน้า feed หลักโดยตรง ระบบเปิด bottom sheet ให้เลือกหมวดหมู่ แล้วอัปเดตทันทีโดยไม่ออกจากหน้า feed
- หน้าจอแก้ไขธุรกรรมแบบเต็มรูปแบบสงวนไว้สำหรับกรณีข้อมูลผิดปกติเท่านั้น (เช่น รายการ "junk" ตาม BR-8 หรือต้องแก้ไขฟิลด์อื่นนอกเหนือจากหมวดหมู่)

---

## 8. ความต้องการเชิงฟังก์ชัน (Functional Requirements)

### FR-1: การจัดการบัญชี (Account Management)

| ID | รายละเอียด |
|---|---|
| FR-1.1 | ผู้ใช้สามารถสร้างบัญชีใหม่ โดยระบุชื่อ, ประเภทบัญชี, ยอดตั้งต้น, คำค้นหาสำหรับจับคู่อัตโนมัติ, และเลือกธนาคาร (`bank_icon`) จาก list ที่ Frontend รู้จัก |
| FR-1.2 | ผู้ใช้สามารถแก้ไขข้อมูลบัญชีที่มีอยู่ รวมถึงคำค้นหาสำหรับจับคู่อัตโนมัติ |
| FR-1.3 | ผู้ใช้สามารถปิดการใช้งานบัญชี (soft delete) โดยไม่ลบประวัติธุรกรรมที่เกี่ยวข้อง |
| FR-1.4 | ระบบแสดงรายการบัญชีทั้งหมดพร้อมยอดคงเหลือปัจจุบันของแต่ละบัญชี (เฉพาะบัญชี active) |
| FR-1.5 🆕 | Frontend ต้องแสดงชื่อและโลโก้ของบัญชีที่ถูกปิดใช้งานแล้วได้ถูกต้องเมื่อปรากฏในธุรกรรมเก่า แม้ backend จะไม่คืนบัญชีนั้นจาก `GET /accounts` อีกต่อไป (ดู BR-10) |

### FR-2: การจัดการหมวดหมู่ (Category Management)

| ID | รายละเอียด |
|---|---|
| FR-2.1 | ผู้ใช้สามารถสร้างหมวดหมู่ใหม่ ระบุชื่อ ประเภท (รายรับ/รายจ่าย) ไอคอน และสี |
| FR-2.2 | ผู้ใช้สามารถแก้ไขและลบหมวดหมู่ที่มีอยู่ หากมีธุรกรรมที่ผูกกับหมวดหมู่นั้นอยู่ ระบบต้องแจ้งเตือนจำนวนรายการที่ได้รับผลกระทบก่อนลบจริง เมื่อลบแล้วธุรกรรมเหล่านั้นจะกลายเป็นสถานะไม่มีหมวดหมู่ (ไม่ลบธุรกรรม) |
| FR-2.3 | ระบบแสดงรายการหมวดหมู่แยกตามประเภท (รายรับ/รายจ่าย) |

### FR-3: การบันทึกธุรกรรมด้วยตนเอง (Manual Entry)

| ID | รายละเอียด |
|---|---|
| FR-3.1 | ผู้ใช้สามารถสร้างรายการรายรับหรือรายจ่ายเอง โดยระบุจำนวนเงิน บัญชี หมวดหมู่ วันที่ และบันทึกเพิ่มเติม |
| FR-3.2 | ผู้ใช้สามารถสร้างรายการย้ายเงินเอง โดยระบุจำนวนเงิน บัญชีต้นทาง บัญชีปลายทาง และวันที่ (หมวดหมู่ไม่บังคับ) |
| FR-3.3 | ผู้ใช้สามารถแก้ไขและลบรายการที่สร้างเองได้เช่นเดียวกับรายการจากสลิป |

### FR-4: การสแกนสลิปอัตโนมัติ (Slip OCR) — 2 ช่องทาง เข้า pipeline เดียวกัน

| ID | รายละเอียด |
|---|---|
| FR-4.1 | ผู้ใช้สามารถถ่ายภาพหรือเลือกภาพสลิปจากคลังภาพ เพื่ออัปโหลดเข้าสู่ระบบด้วยตนเอง |
| FR-4.2 | ระบบสแกนคลังภาพในเครื่องหาไฟล์ใหม่โดยอัตโนมัติตอนเปิดแอปและตอนกลับมาจาก background โดยไม่ต้องมีการกดปุ่มใดๆ (ตาม BR-8) เฉพาะ album ที่กำหนด |
| FR-4.3 | ระบบส่งภาพให้ Gemini AI อ่านข้อมูล: จำนวนเงิน, ชื่อผู้โอน, ชื่อผู้รับ, ข้อความธนาคาร, เวลาโอน |
| FR-4.4 | ระบบจำแนกประเภทธุรกรรมอัตโนมัติตาม BR-1 |
| FR-4.5 | ระบบจับคู่บัญชีอัตโนมัติตาม BR-2 |
| FR-4.6 | ระบบป้องกันการสแกนไฟล์ซ้ำ ตาม BR-6 |
| FR-4.7 | ระบบบันทึกธุรกรรมลงฐานข้อมูลทันทีตาม BR-3 |
| FR-4.8 ✏️ | หากภาพที่ประมวลผลไม่ใช่สลิปที่มีข้อมูลอ่านได้ ระบบ (backend) ยังคงบันทึกธุรกรรมตามปกติ (amount=0) — Frontend เป็นผู้ตรวจจับและแสดงเป็นรายการ "junk" พร้อม badge เตือนแทนการข้ามแบบเงียบๆ (ดู BR-8 revised) |
| FR-4.9 🆕 | Frontend ต้องจำกัดอัตราการยิง request ไปยัง `/upload-slip` ให้ไม่เกิน rate limit ของ backend (10 ครั้ง/นาที) โดย throttle แบบ sequential พร้อม delay ระหว่างไฟล์ และแสดง progress ระหว่างประมวลผลหลายไฟล์ |

### FR-5: การจัดการธุรกรรม (Transaction Management)

| ID | รายละเอียด |
|---|---|
| FR-5.1 | ผู้ใช้สามารถดูรายการธุรกรรมทั้งหมด กรองตามเดือน/ปี พร้อมแบ่งหน้า (pagination) |
| FR-5.2 | ผู้ใช้สามารถแก้ไขจำนวนเงิน หมวดหมู่ บัญชี บันทึกเพิ่มเติม และวันที่ของธุรกรรมที่มีอยู่ ผ่านหน้าจอแก้ไขแบบเต็มรูปแบบ (ประเภทธุรกรรมเป็นค่าอ่านอย่างเดียว ไม่สามารถแก้ไขได้ในหน้านี้) |
| FR-5.3 | ผู้ใช้สามารถกำหนดหรือเปลี่ยนหมวดหมู่ของธุรกรรมได้อย่างรวดเร็วโดยไม่ต้องเปิดหน้าจอแก้ไขแบบเต็ม (ตาม BR-9) เนื่องจากธุรกรรมส่วนใหญ่ที่มาจากสลิปไม่มีหมวดหมู่ตั้งแต่แรก |
| FR-5.4 | ผู้ใช้สามารถลบธุรกรรมที่ไม่ต้องการ พร้อมข้อความยืนยันก่อนลบจริง |
| FR-5.5 🆕 | ผู้ใช้สามารถกำหนด/เปลี่ยนหมวดหมู่ของธุรกรรมได้จากปุ่ม/chip บนแถวของ transaction ในหน้า feed หลักโดยตรง โดยไม่ต้องเข้าหน้าจอแก้ไขแบบเต็ม (ดู BR-11) |

### FR-6: แดชบอร์ดสรุปผล (Dashboard Summary)

| ID | รายละเอียด |
|---|---|
| FR-6.1 | ระบบแสดงยอดรวมรายรับและรายจ่ายแยกรายเดือนหรือรายปี พร้อม breakdown ตามหมวดหมู่ |
| FR-6.2 | ระบบแสดงยอดรวมการย้ายเงิน (transfer) แยกต่างหากจากยอดรายรับ-รายจ่าย ตาม BR-4 |
| FR-6.3 | ระบบแสดงยอดคงเหลือปัจจุบันของแต่ละบัญชี |
| FR-6.4 | ผลสรุปถูก cache ไว้ใน Redis เพื่อลดภาระฐานข้อมูล และถูกล้าง cache อัตโนมัติเมื่อมีธุรกรรมใหม่/แก้ไข/ลบที่กระทบช่วงเวลานั้น |

### FR-7: การยืนยันตัวตน (Authentication)

| ID | รายละเอียด |
|---|---|
| FR-7.1 | ทุกคำขอไปยัง endpoint ภายใต้ `/api/v1` ต้องแนบ HTTP header `X-API-Key` ที่ตรงกับค่าที่กำหนดไว้ในระบบ มิฉะนั้นระบบตอบกลับสถานะ 401 |
| FR-7.2 | Endpoint สำหรับตรวจสอบสถานะระบบ (`/health`, `/metrics`, `/swagger`) ไม่ต้องยืนยันตัวตน |

---

## 9. รายการ API Endpoint

| Method | Path | คำอธิบาย | Auth |
|---|---|---|---|
| GET | /health | ตรวจสอบสถานะระบบ | ไม่ต้อง |
| GET | /metrics | Metrics สำหรับ monitoring | ไม่ต้อง |
| GET | /api/v1/transactions | ดึงรายการธุรกรรมตามเดือน/ปี พร้อม pagination | ต้องมี |
| POST | /api/v1/transactions | สร้างรายการธุรกรรมเอง (income/expense/transfer) | ต้องมี |
| GET | /api/v1/transactions/summary | ดึงข้อมูลสรุปรายเดือน/รายปี | ต้องมี |
| POST | /api/v1/transactions/upload-slip | อัปโหลดภาพสลิปเพื่อบันทึกธุรกรรมอัตโนมัติ (rate limit 10/นาที) | ต้องมี |
| PATCH | /api/v1/transactions/:id | แก้ไขธุรกรรม | ต้องมี |
| DELETE | /api/v1/transactions/:id | ลบธุรกรรม | ต้องมี |
| GET | /api/v1/accounts | ดึงรายการบัญชี active ทั้งหมดพร้อมยอดคงเหลือ | ต้องมี |
| POST | /api/v1/accounts | สร้างบัญชีใหม่ | ต้องมี |
| PATCH | /api/v1/accounts/:id | แก้ไขบัญชี | ต้องมี |
| DELETE | /api/v1/accounts/:id | ปิดการใช้งานบัญชี (soft delete) | ต้องมี |
| GET | /api/v1/categories | ดึงรายการหมวดหมู่ตามประเภท | ต้องมี |
| POST | /api/v1/categories | สร้างหมวดหมู่ใหม่ | ต้องมี |
| PATCH | /api/v1/categories/:id | แก้ไขหมวดหมู่ | ต้องมี |
| DELETE | /api/v1/categories/:id | ลบหมวดหมู่ | ต้องมี |

> ✏️ **แก้ไขจาก v1:** v1 มี `GET /api/v1/accounts/:id` อยู่ในรายการ — endpoint นี้**ไม่มีอยู่จริง**ในโค้ดและตัดสินใจไม่เพิ่ม เพราะ `GET /accounts` (list) มีฟิลด์ครบทุกตัวอยู่แล้ว Frontend จึงดูรายละเอียดบัญชีจาก local cache แทนได้โดยไม่ต้องยิง request ใหม่

---

## 10. ความต้องการที่ไม่ใช่เชิงฟังก์ชัน (Non-Functional Requirements)

| ID | หัวข้อ | รายละเอียด |
|---|---|---|
| NFR-1 | Performance | การประมวลผลสลิปด้วย Gemini ทำงานแบบ synchronous ภายใน request เดียว ระยะเวลาตอบสนองที่ยอมรับได้ประมาณ 2–5 วินาทีต่อครั้ง |
| NFR-2 | Security | ป้องกันการเข้าถึงด้วย static API Key เพียงอย่างเดียว เหมาะสมกับการใช้งานคนเดียว ไม่มีระบบ session หรือ token หมดอายุ |
| NFR-3 | Data Integrity | ฐานข้อมูลไม่มีข้อมูลจริงอยู่ก่อน สามารถปรับโครงสร้าง (schema) ได้อย่างอิสระโดยไม่ต้องเขียน migration ย้ายข้อมูลเก่า |
| NFR-4 | Caching | Redis ใช้เพื่อ cache ผลสรุปแดชบอร์ดและป้องกันการสแกนซ้ำเท่านั้น ไม่ใช่เป็น job queue ในเวอร์ชันนี้ |
| NFR-5 | Extensibility | โครงสร้างข้อมูล Account เตรียมรองรับประเภท investment เพื่อขยายเป็นฟีเจอร์ติดตามการลงทุนในอนาคตโดยไม่ต้องปรับโครงสร้างฐานข้อมูลใหม่ |
| NFR-6 ✏️ | Reliability | ระบบมี rate limiting ป้องกันการเรียก API ถี่เกินไป: endpoint ทั่วไปภายใต้ `/api/v1` จำกัด 60 ครั้ง/นาทีต่อ IP, endpoint `/upload-slip` จำกัดเพิ่มเติมอีกชั้นที่ 10 ครั้ง/นาที — Frontend ต้อง throttle การอัปโหลดสลิปให้สอดคล้องกับขีดจำกัดนี้เสมอ (ดู FR-4.9) |
| NFR-7 | Usability | แอป Flutter ต้องรองรับการถ่ายภาพ/เลือกภาพจากคลังภาพเพื่ออัปโหลดสลิปได้สะดวกในไม่กี่ขั้นตอน |

---

## 11. แผนสำหรับเฟสถัดไป (Future Scope)

รายการต่อไปนี้ถูกพิจารณาแล้วแต่ตัดสินใจไม่รวมในเวอร์ชันนี้เพื่อไม่ให้ขอบเขตงานบวมเกินไป:

1. **ฟีเจอร์วิเคราะห์การลงทุน:** คำนวณผลตอบแทน ต้นทุน กำไร/ขาดทุน ของบัญชีประเภท investment (เช่น Dime-FCD)
2. **Async processing:** เปลี่ยนการประมวลผลสลิปเป็นแบบ asynchronous ผ่าน Redis job queue หากปริมาณการใช้งานเพิ่มขึ้นจนกระทบความเร็ว
3. **ระบบยืนยันตัวตนแบบเต็มรูปแบบ:** หากในอนาคตต้องเปิดให้ผู้ใช้คนอื่นเข้าถึงระบบ
4. **รองรับ iOS:** ต้องออกแบบใหม่สำหรับข้อจำกัด Limited Photo Library ที่ขัดกับ auto-scan (BR-8)

---

## 12. ภาคผนวก: บันทึกการตัดสินใจออกแบบ (Design Decision Log)

| หัวข้อ | การตัดสินใจ | เหตุผล |
|---|---|---|
| ความสัมพันธ์กับโค้ดเดิม | ต่อยอดจาก cashlog-api มีระบบ OCR สลิปทำงานได้อยู่แล้ว | - |
| จำนวนผู้ใช้ | Single-user ใช้งานส่วนตัวคนเดียว | ไม่จำเป็นต้องมีระบบสมาชิก |
| โครงสร้างบัญชี | เพิ่ม Account entity ใหม่ทั้งหมด | จำเป็นเพื่อให้ "ย้ายเงิน" มีความหมาย |
| รูปแบบข้อมูล transfer | เก็บเป็น 1 แถวที่มี from/to account | ง่ายกว่าการทำ double-entry แบบ 2 แถว |
| การจำแนกประเภทอัตโนมัติ | เทียบชื่อผู้โอน/รับกับชื่อเจ้าของระบบ | ใช้ข้อมูลที่ Gemini อ่านได้อยู่แล้ว โดยไม่ต้องเพิ่มต้นทุน |
| กรณีจำแนกไม่ได้ | ตั้งเป็น expense โดยไม่ระบุหมวดหมู่ | ให้สังเกตเห็นและแก้ไขได้ง่ายในภายหลัง |
| การจับคู่บัญชีอัตโนมัติ | ใช้ keyword จากข้อความธนาคารบนสลิป แต่ไม่รับประกันความถูกต้อง | ข้อจำกัดของพร้อมเพย์และการโอนภายในบัญชี Dime-FCD ที่ไม่มีข้อมูลธนาคารปรากฏ |
| ขั้นตอนก่อนบันทึก | ไม่มีขั้นตอนยืนยัน บันทึกอัตโนมัติทันทีเสมอ | เน้นความเร็วและความครบถ้วนเป็นหลัก |
| การยืนยันตัวตน | Static API Key | เพียงพอสำหรับผู้ใช้คนเดียว ไม่คุ้มที่จะทำระบบ login เต็มรูปแบบ |
| การใช้งาน Redis | Cache เท่านั้น ไม่ทำ job queue | ความเร็วของ Gemini Flash ในปัจจุบันเพียงพอแล้ว |
| ข้อมูลในฐานข้อมูลปัจจุบัน | รีเซ็ตโครงสร้างใหม่ได้อิสระ | ยังไม่มีข้อมูลการใช้งานจริง |
| บัญชี Dime-FCD | แยกเป็นบัญชีต่างหาก | เผื่อรองรับฟีเจอร์วิเคราะห์การลงทุนในอนาคต |
| หมวดหมู่สำหรับ transfer | ไม่บังคับ | ความหมายของรายการสื่อสารผ่านบัญชีต้นทาง-ปลายทางอยู่แล้ว |
| ยอดคงเหลือต่อบัญชี | คำนวณสดจาก opening balance และประวัติธุรกรรม | ไม่เก็บ field แยกเพื่อป้องกันข้อมูลไม่ตรงกัน |
| สถาปัตยกรรม Flutter | Clean Architecture + Riverpod + dartz Either | สอดคล้องกับแนวทางที่ใช้ในโปรเจกต์อื่นของผู้พัฒนา |
| ช่องทางรับสลิป | สแกนอัตโนมัติตอนเปิดแอป (MeowJot-style) เป็น default + ปุ่มถ่าย/เลือกรูปเดิมยังใช้งานคู่กัน | รายจ่ายมักอยู่ในโฟลเดอร์ที่สแกนถึง แต่รายรับอาจตกหล่น ต้องมีทางเลือกสำรอง |
| การกำหนดหมวดหมู่ | เพิ่มช่องทางกำหนดหมวดหมู่แบบเร็ว แยกจากหน้าจอแก้ไขเต็มรูปแบบ | ธุรกรรมจากสลิปไม่มีหมวดหมู่ตั้งแต่แรกเสมอ ต้องทำให้เติมข้อมูลได้เร็วที่สุด |
| การแก้ไขประเภทธุรกรรม | ไม่อนุญาตให้แก้ไขประเภทธุรกรรมหลังสร้างแล้ว | ป้องกันความซับซ้อนจากการเปลี่ยนแปลง field ที่เกี่ยวข้องจำนวนมาก |
| **Account icon/color** 🆕 | รวม `icon_key`+`color_hex` เป็น `bank_icon` เดียว, Frontend เป็นเจ้าของชื่อ/โลโก้/สีผ่าน local map | ลดความซับซ้อน backend, ให้ Frontend ปรับ branding ได้อิสระโดยไม่ต้อง migrate ข้อมูล |
| **ภาพที่ไม่ใช่สลิป** 🆕 | Backend ไม่ skip เงียบๆ — สร้าง transaction ปกติ (amount=0), Frontend detect+badge+ให้ลบ/แก้เอง | ป้องกันข้อมูลสูญหายแบบไม่รู้ตัวหาก Gemini ตีความสลิปจริงผิดพลาด |
| **Account cache sync** 🆕 | Upsert-only ไม่ลบ record ที่หายจาก response | GET /accounts คืนเฉพาะบัญชี active เท่านั้น ต้องเก็บบัญชีที่ปิดแล้วไว้ lookup ธุรกรรมเก่า |
| **การอัปโหลดสลิปหลายไฟล์** 🆕 | Sequential upload พร้อม delay ~7 วินาที/ไฟล์ | backend จำกัด rate limit 10 request/นาทีเฉพาะ /upload-slip |
